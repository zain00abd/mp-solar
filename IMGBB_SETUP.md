# إعداد خدمة ImgBB لرفع الصور

## ما هي ImgBB؟
ImgBB هي خدمة مجانية لاستضافة الصور على السحابة، تساعدك في:
- ✅ تقليل حجم قاعدة البيانات بشكل كبير
- ✅ تحميل أسرع للمنتجات والصفحات
- ✅ استضافة غير محدودة للصور (مجاناً)

## خطوات الإعداد

### 1. الحصول على مفتاح API

1. اذهب إلى: https://api.imgbb.com/
2. قم بتسجيل الدخول أو إنشاء حساب جديد (مجاني)
3. ستحصل على مفتاح API الخاص بك مباشرة

### 2. إضافة المفتاح إلى المشروع

قم بإنشاء ملف باسم `.env.local` في المجلد الرئيسي للمشروع:

```bash
# .env.local
NEXT_PUBLIC_IMGBB_API_KEY=ضع_مفتاحك_هنا
```

مثال:
```bash
NEXT_PUBLIC_IMGBB_API_KEY=abc123def456ghi789jkl012mno345pqr
```

### 3. إعادة تشغيل السيرفر

بعد إضافة الملف، قم بإعادة تشغيل السيرفر:

```bash
npm run dev
```

## كيف يعمل؟

1. **المستخدم يختار صورة** → يتم ضغطها إلى WebP
2. **رفعها تلقائياً إلى ImgBB** → يتم الحصول على رابط الصورة
3. **حفظ الرابط في قاعدة البيانات** بدلاً من الصورة نفسها

## ملاحظات مهمة

- ⚠️ **إذا لم تضف المفتاح:** سيعمل النظام عادياً ولكن سيحفظ الصور كـ base64 في قاعدة البيانات
- 💡 **الخدمة مجانية تماماً:** لا يوجد حد أقصى لعدد الصور
- 🔒 **آمن:** المفتاح يبقى في ملف `.env.local` ولن يتم رفعه إلى GitHub

## الفوائد

### قبل ImgBB:
- 📦 صورة 300KB → تخزن في قاعدة البيانات
- ⏳ تحميل بطيء عند جلب المنتجات
- 💾 قاعدة بيانات كبيرة جداً

### بعد ImgBB:
- 🌐 صورة 300KB → تخزن على السحابة
- ⚡ تحميل سريع جداً
- 💾 قاعدة بيانات صغيرة (فقط رابط الصورة)

## مثال على الفرق:

```
100 منتج:
❌ بدون ImgBB: ~30MB في قاعدة البيانات
✅ مع ImgBB: ~10KB في قاعدة البيانات (فقط الروابط)
```

---

للمزيد من المعلومات: https://api.imgbb.com/

